import * as React from "react";
import { Grid } from "react-bootstrap";
import { RegisterAdminForm } from "../register";

export const RegisterAdminPage = () => {
	return (
		<Grid>
			<RegisterAdminForm />
		</Grid>
	);
};
